import './App.css';


// element.className = class
// 리액트는 독자적인 element(jsx)를 구성한다. (html element가 아니다.)
// 리액트의 element는 html element로 render로 다시 출력되기 때문에 그 구성이 비슷함
// 자바스크립트에서 html element를 불러올때 사용하는 이름과 비슷.

function App() {
  return (
    <div className="App">
      test
    </div>
  );
}

export default App;
